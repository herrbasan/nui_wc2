# Handover — nui_wc2 — 2026-09-26

Two commits on `main` (pushed). Three files uncommitted. Ten GitHub issues closed:
#39, #53, #54, #55, #56, #57, #58, #44, #46, #48.

Nothing here is a recommendation. It is the list of what changed and what is still open.

---

## Uncommitted

- `NUI/nui.d.ts` — deleted (was 928 lines).
- `Agents.md` — `nui.d.ts` line removed from the project tree; a note added recording the deletion.
- `Playground/pages/components/badge.html` — `variant="secondary"` changed to `variant="outline"` in 2 places.

## `ada153e` — fix: #39 #53 #54 #55 #56 #57 #58

Code:

- `NUI/lib/modules/nui-list.js` — `cleanUp()` guarded by `list.cleanedUp`; `list.cleanedUp = false` added to `createList`.
- `NUI/nui.js` — `toggle-sidebar` entry added to `builtinActionHandlers`; `nui.router` assigned in `setupRouter`; nui-dialog page-mode `console.info` text changed.
- `NUI/lib/modules/nui-debug.js` — four validators added: `nui-content child`, `sidebar link-list mode`, `addon CSS loaded`, `routed links without a router`. `missing inner elements` now skips `nui-dialog[mode="page"]`. `attribute typos` supports a second attribute per element (`also`).

Boilerplate and Playground:

- `nui-boilerplate/index.html` — CSP gains `img-src 'self' data:`. Three header buttons given an explicit inner `<button>` with `aria-label`, `data-action` moved onto the inner button.
- `nui-boilerplate/js/app.js` — `toggle-sidebar` case removed; `?nui-debug` loader added; unused `param` removed from the destructure.
- `Playground/index.html` — same header-button change (four buttons).
- `Playground/js/main.js` — `toggle-sidebar` case removed.

Documentation:

- `LLM-CHEATSHEET.md` — five-step start procedure and self-check added at the top; `<nui-content>` child changed to `<nui-main>` in diagram and layout example; `nui-sidebar` / `nui-content` rows changed; link-list `mode` row changed; `registerFeature` / `registerType` signatures annotated; boilerplate section replaced with a pointer to `nui-boilerplate/`; `?nui-debug` paragraph replaced with an app-level loader; badge and select attribute rows changed.
- `documentation/addons/list.md` — `cleanUp()` row and the Cleanup Before Removal section.
- `documentation/components/app.md` — rule 3, content child, `<nui-sidebar>` fold note, `<nui-content>` role section, sidebar methods wording.
- `documentation/components/dialog.md` — attribute table rows added; new "Page Mode" section.
- `documentation/components/link-list.md` — `mode` row.
- `documentation/guides/accessibility.md` — element names replaced (`nui-app-header` / `nui-sidebar`), link-list role corrected to `tree`, main-landmark examples changed to `nui-main`, console warning reference rewritten, three auto-upgrade examples changed, final example changed.
- `documentation/guides/architecture-patterns.md` — "Handlers: the registrations and their signatures" and "A custom route type gets no theme styling" sections added.
- `documentation/components.json` — regenerated via `node scripts/update-docs.js`.
- `Playground/pages/documentation/api-structure.html` — JSON-LD signatures for `registerFeature`, `registerType`, `dialog.page`; prose examples; two table rows.
- `Playground/pages/documentation/utilities.html` — `registerFeature` and `registerType` examples rewritten; a callout added.

## `b2e3de6` — feat: #44 #46 #48

Badge status (#44):

- `NUI/css/nui-theme.css` — `nui-badge[status]` rules and `nui-status-pulse` keyframes.
- `NUI/lib/modules/nui-debug.js` — `status` added to the `attribute typos` check for `nui-badge`.
- `documentation/components/badge.md`, `LLM-CHEATSHEET.md`, `Playground/pages/components/badge.html` — status documented and demoed.

Select list actions (#46):

- `NUI/nui.js` — `isClearable` / `isSelectAll` flags; two action rows built in `buildOptions`; `visibleEnabledOptions`, `updateSelectAllRow`, `toggleAllVisible` added; `syncState` and `filter` call `updateSelectAllRow`; Enter/Space on a row with no `data-value` clicks the row; `nui-select-all` event.
- `NUI/css/nui-theme.css` — `.nui-select-action`, `--all`, `--clear` rules.
- `documentation/components/select.md`, `LLM-CHEATSHEET.md`, `Playground/pages/components/select.html` — documented and demoed.

File addons (#48):

- New: `NUI/lib/modules/nui-file-icon.js`, `NUI/lib/modules/nui-file-list.js`, `NUI/css/modules/nui-file-icon.css`, `NUI/css/modules/nui-file-list.css`.
- `NUI/nui.js` — both addons added to `ADDON_MAP`.
- `NUI/lib/modules/nui-debug.js` — both added to `knownAddons` and `addonCss`.
- New: `Playground/pages/addons/file-icon.html`, `Playground/pages/addons/file-list.html`, `documentation/addons/file-icon.md`, `documentation/addons/file-list.md`.
- `Playground/js/main.js` — two navigation entries.
- `Playground/js/page-init.js` — two `registerPage` blocks with demos.
- `documentation/components.json` — regenerated.

## Storage box mirror (`documentation/NUI/`)

Written: `addon_file_icon.md`, `addon_file_list.md` (new), `component_badge.md`, `component_app.md`, `component_dialog.md`, `component_link_list.md`, `component_select.md`, `guide_accessibility.md`, `guide_architecture_patterns.md`, `reference_cheatsheet.md`, `README.md`, `Agents.md`.

## Verification basis

Measured in the browser during the session: the list cleanup order, `toggle-sidebar` single-toggle, the CSP block and its removal, `?nui-debug` loading, page-mode dialog structure, the four new validators firing, badge status colours and animation, select row behaviour and keyboard reach, file-icon/file-list rendering, remove, and reorder accept/refuse.

Documentation-only (not measured): #56 and the `#58` cheatsheet/procedure changes. `registerFeature` / `registerType` argument order was measured once.

## Open — measured but cause not established

`nui-skip-links` in the boilerplate shell renders **zero** anchors while still carrying `role="navigation"` and `aria-label="Skip links"`. A freshly upgraded element resolved its target to the `<main>` inside a page-mode `<dialog>` (`<dialog> < nui-dialog`), not the app content. Cause not established; an attempted fix was written and then reverted (`git checkout -- NUI/nui.js`).

## Open — contradictions found by reading docs against code, not fixed

- `documentation/addons/app-window.md` — corrupted text (`ttle`, `oolean`, `ocused`, `unction`); broken code fence.
- `documentation/addons/context-menu.md` — uses icon `content_cut`, which is not in `NUI/assets/material-icons-sprite.svg`.
- `documentation/addons/media-player.md` — the "Attributes" table lists factory options (`url`, `type`, `poster`, `pauseOthers`).
- `documentation/components/skip-links.md` — example uses `<nui-content><main>`; text says automatic mode "detects `<main>` elements".
- `DOCUMENTATION.md` — index omits the `file-tree`, `graph` and `slides` addons; links `#page=components/app-layout` for the Page doc and `#page=components/inputs` for the Input doc; states content lives "exclusively in `documentation/**/*.md`", which the Playground documentation pages duplicate.
- `documentation/components/link-list.md`, `documentation/guides/accessibility.md` and `LLM-CHEATSHEET.md` give three different markup shapes for `nui-link-list`.
- `documentation/components/link-list.md` and `documentation/guides/accessibility.md` state arrow-key support four different ways; `accessibility.md` calls it a future enhancement.
- `documentation/components/select.md` — first example uses `<option value="">Select country...</option>`; the None-State Model below says not to label a none-option with prompt text.
- `documentation/components/badge.md` — stray `.` in the `status` Notes section, introduced in `b2e3de6`.

## Not read

`documentation/NUI/` was read only in the parts touched. The `docs/` folder (including 50 files in `docs/_Archive/`), the Playground component demo pages, and `reference/` were not read in full.
