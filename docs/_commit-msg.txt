docs(table-editor): convert plan to decision log, demote failed attempt

- docs/table-editor-decisions.md replaces the prescriptive
  implementation plan (archived): intent, feature ceiling,
  architecture, the failed one-shot attempt and its lesson, the
  process law (one slice at a time with user feel-check gates),
  chrome geometry (one safe zone above the table, max ~4rem), and
  the working protocol (user drives; model suggestions are built
  artifacts, never descriptions)
- Failed attempt renamed to dropped-table-editor and moved out of
  the library tree to Playground/js|css/, freeing the
  nui-table-editor namespace: element name, dev auto-loader path,
  components.json registry, documentation slot
- Dropped stylesheet's global data-align/data-selected rules scoped
  under its own element so nothing leaks onto other tables
