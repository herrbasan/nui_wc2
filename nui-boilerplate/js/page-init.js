// Page registrations using nui.registerPage() — the recommended pattern.
// Each registration links an HTML fragment to its init logic.
// Import this file in your app.js.

import { nui } from '../../NUI/nui.js';

// Example: register the home page
// nui.registerPage('home', {
//     html: 'home.html',
//     init(element, params, nui) {
//         element.querySelector('nui-button').addEventListener('click', () => {
//             console.log('Button clicked!');
//         });
//     }
// });
